# S200 LCEDA MCP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a stdio MCP (`s200-lceda`) with 6 task tools that read/write S200 `.epru` and diagnose Hub without depending on EDA Bridge.

**Architecture:** Single Python package under `mcp/s200_lceda/` using MCP SDK 2.x `MCPServer`. Core epru logic in `lib/epru.py`; Hub probe in `lib/hub.py`. Direct-write policy.

**Tech Stack:** Python 3.11, `mcp>=2.2`, stdio transport.

---

### Task 1: Package + constants + epru helpers

**Files:**
- Create: `mcp/s200_lceda/lib/constants.py`
- Create: `mcp/s200_lceda/lib/epru.py`
- Create: `mcp/s200_lceda/lib/hub.py`
- Create: `mcp/s200_lceda/server.py`
- Create: `mcp/s200_lceda/README.md`

- [ ] Implement pad inject / footprint bind / status scan (ported from `tools/fix_x25_pads_surgical.py`)
- [ ] Register 6 tools on MCPServer
- [ ] Wire `~/.cursor/mcp.json`
- [ ] Smoke: `project_status` + `hub_bridge_status` via Python call
